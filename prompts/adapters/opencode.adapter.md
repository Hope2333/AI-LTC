# OpenCode Adapter Delta

- keep OpenCode runtime details here instead of the stable role files
- use this layer for plugin and hook specific deltas
- route platform details to runtime-facing docs when they exceed prompt scope
