# Language Policy Example

Working language:
- `English`

Human-facing summary language:
- `Chinese`

Human input language policy:
- `auto-detect`

Recommended rule:
- keep technical payloads, file paths, schema fields, commands, and machine-readable state in English
- let human-facing summaries follow the configured summary language
- ask for the summary language first during init when it is not already configured
